# Footnotes

This note is referenced twice[^shared].

The second reference should reuse it[^shared], while this note is separate[^other].

[^shared]: This explanation is shared by both references.

[^other]: A separate footnote with **formatting**.
